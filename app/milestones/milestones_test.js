'use strict';

describe('myApp.milestones module', function() {

  beforeEach(module('myApp.milestones'));

  describe('milestones controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var milestonesCtrl = $controller('milestonesCtrl');
      expect(milestonesCtrl).toBeDefined();
    }));

  });
});