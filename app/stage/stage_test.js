'use strict';

describe('myApp.stage module', function() {

  beforeEach(module('myApp.stage'));

  describe('stage controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var stageCtrl = $controller('stageCtrl');
      expect(stageCtrl).toBeDefined();
    }));

  });
});